# Plugins

This directory will hold WebAssembly plugin examples and build artifacts.

Planned targets:

- Rust -> `wasm32-wasi`
- TinyGo -> WASI-compatible `.wasm`

## Rust Plugin
Use `wasm32-wasi` target for plugin builds.
